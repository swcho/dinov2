Educational infographic, clean flat design, 16:9 landscape aspect ratio, white background, soft pastel palette (grey, orange, teal accents), thin sans-serif labels, high text legibility.

Title at top center, bold: "DINOv2 Data Pipeline: 3 Stages" with a small Korean subtitle "데이터 파이프라인 3단계".

Layout: a single left-to-right horizontal flow divided by two dashed vertical separators into three numbered panels, with input on the far left and output on the far right. Eye flow strictly left to right.

Far left input column: a large grey cylinder database labeled "Uncurated Web Images (1.2B)" stacked above a small orange cylinder labeled "Curated Datasets (ImageNet etc.)". Both send arrows rightward into panel 1.

Panel 1, badge "1", header "Embedding (임베딩)": each photo thumbnail turns into a small vector bar icon. Sublabel "Self-supervised ViT-H/16" and "Cosine similarity".

Panel 2, badge "2", header "Deduplication (중복제거)": only the grey uncurated row passes through; three near-identical thumbnails grouped, two marked with red X, one kept. Sublabel "Remove near-duplicates" and "1.3B -> 744M".

Panel 3, badge "3", header "Retrieval (검색)": an orange curated image inside a bold query box with a magnifying glass, arrows pulling in matching grey thumbnails; an unrelated thumbnail crossed out with red X. Sublabel "Match uncurated to curated" and "kNN N=4 / k-means clusters".

Far right output column: a large cylinder that is two-tone, orange bottom labeled "Curated" and grey top labeled "Retrieved", overall label "LVD-142M (142M images)" with a small check mark "Augmented Curated Data".

Bottom strip, small three-word tagline: "Embed -> Dedup -> Retrieve".

Keep all labels short, no long sentences, no extra decorative text.
