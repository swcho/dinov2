Educational infographic, clean flat design, 16:9 landscape, soft off-white background, muted palette (slate blue for text-guided side, warm orange for self-supervised side), crisp sans-serif labels, minimal icons, no photorealism.

Title at top center, bold: "Limits of Text-Guided Pretraining" with smaller Korean subtitle "텍스트 지도 사전학습의 한계".

Layout: two large side-by-side comparison columns occupying the middle, plus a slim bottom strip. Eye flow: title, then left column, then right column, then bottom strip.

LEFT COLUMN (slate blue header "Text-Guided (CLIP)"):
- Top: a photo-like thumbnail of a dog on a sofa in a room, drawn as a rich detailed flat illustration, with a caption bubble under it reading only "a dog on a sofa".
- A funnel icon between the image and the caption bubble, labeled "Caption = approximation" and Korean "캡션은 근사치".
- Below, a row of three faded, crossed-out small icons labeled "Depth", "Boundaries", "Part matching", with a label "Pixel-level info lost".
- Bottom: two chain-linked cards, one image icon and one text icon, connected by a padlock, labeled "Needs image-text pairs" and Korean "정렬 데이터 필수".

RIGHT COLUMN (warm orange header "Self-Supervised (DINOv2)"):
- Top: the same dog-on-sofa illustration, but with a colorful heatmap overlay and a segmentation outline, labeled "Learns from pixels".
- A wide open channel icon (no funnel) labeled "Full image info kept".
- Below, the same three small icons but bright and checked, labeled "Depth", "Boundaries", "Part matching".
- Bottom: a stack of plain image cards with no text attached, labeled "Raw images only" and Korean "이미지만으로 학습".

BOTTOM STRIP: a small dual-bar mini chart with two grouped bars, group labels "Classification" and "Depth / Segmentation"; blue and orange bars nearly equal for Classification, orange bar clearly better than blue for Depth / Segmentation. Caption "Gap appears at pixel level".

All labels short, high contrast, legible; no long sentences, no extra text.
